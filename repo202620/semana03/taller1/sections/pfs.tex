\subsection{Puntos fijos de un arreglo}
Sea $A$ un arreglo de longitud $n$ cuyas entradas son números enteros y tal que
\[
	A[0] < A[1] < \ldots < A[n - 1]. 
\]
Queremos resolver el problema de encontrar un índice i tal que A[i] = i, si es que
existe.
